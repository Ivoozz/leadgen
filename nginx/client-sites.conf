server {
    listen 80;
    server_name ~^(?<subdomain>.+)\.sites\.localhost$;

    root /var/www/client-sites/$subdomain;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
